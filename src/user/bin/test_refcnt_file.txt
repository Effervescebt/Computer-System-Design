Student demonstrates reference counting with the following type of user program / 9 pts
• User program opens a file from the file system then forks into a parent and child program
• Parent program closes file and waits for child to exit
• Child program conducts reads and writes on the open file, closes the file and then exits
• Parent program exits
• Note: The parent closing the file shouldn’t affect the child